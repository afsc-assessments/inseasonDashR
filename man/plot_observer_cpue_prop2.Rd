% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_observer_cpue_prop2.R
\name{plot_observer_cpue_prop2}
\alias{plot_observer_cpue_prop2}
\title{Plot observer CPUE indices filtered by species proportion (with optional council-blend weighting)}
\usage{
plot_observer_cpue_prop2(
  con,
  species,
  prop_min = 0.3,
  date_min = NULL,
  date_max = NULL,
  year_min = NULL,
  region = NULL,
  gear = c("Trawl", "Pot", "Longline"),
  plot_type = c("MONTH", "GEAR", "YEAR"),
  use_blend = TRUE,
  sql_dir = "sql",
  base_size = 16
)
}
\arguments{
\item{con}{Connection object. Recommended: a named list with at least \code{afsc},
and (if \code{use_blend=TRUE}) also \code{akfin}. Example: \code{list(afsc=afsc, akfin=akfin)}.
If you pass a single DBI connection, it is assumed to be the AFSC connection.}

\item{species}{One or more agency species codes (numeric/integer).}

\item{prop_min}{Minimum species proportion of total catch required to retain a haul
(0--1). Default 0.30.}

\item{date_min}{Optional start date (inclusive) as "mm/dd/yyyy".}

\item{date_max}{Optional end date (inclusive) as "mm/dd/yyyy".}

\item{year_min}{Optional minimum YEAR for SQL filter (YEAR >= year_min). If NULL and
\code{date_min} is provided, it is derived from \code{date_min}.}

\item{region}{Optional region/area selector: one or more of "AI","BS","GOA","BSWGOA"
or explicit NMFS area codes (numeric). If NULL, no area filtering is applied.}

\item{gear}{Gear selector: one or more of "Trawl","Pot","Longline". Default all.}

\item{plot_type}{Which aggregation to plot: "MONTH", "GEAR", or "YEAR".}

\item{use_blend}{Logical; if TRUE, apply council-blend weighting using AKFIN pulls.
Requires \code{con$akfin} and the SQL files \code{GET_CODES.sql} and \code{GET_BLEND.sql}.}

\item{sql_dir}{Directory containing SQL files. Default "sql".}

\item{base_size}{ggplot base font size. Default 16.}
}
\value{
A list with components:
\itemize{
\item data_obs: filtered haul-level observer data with CPUE fields
\item data_index: indexed/aggregated data used for plotting
\item plots: named list of ggplot objects (weight + number when available)
}
}
\description{
Pulls observer haul/species composition data using \code{GET_CURRENT.sql} (via your
\code{sql_filter()} + \code{sql_run()} utilities), filters hauls by a minimum proportion
of the selected species in total catch, computes effort and CPUE, and then
produces CPUE index plots.
}
\details{
Optionally, the function can compute "council blend" weights using \code{GET_CODES.sql}
\itemize{
\item \code{GET_BLEND.sql} (AKFIN) and apply those weights to observer CPUE before
building indices (this mirrors the logic in your current script). :contentReference[oaicite:5]{index=5}
}

Species proportion is computed as:
\deqn{prop = (weight\_kg/1000) / official\_total\_catch\_mt}

Effort is computed by gear:
\itemize{
\item Trawl (GEAR_TYPE 1--5): duration in minutes = RETRIEVAL_DATE - DEPLOYMENT_DATE
\item Pot (GEAR_TYPE 6) and Longline (GEAR_TYPE 8): TOTAL_HOOKS_POTS
}
}
