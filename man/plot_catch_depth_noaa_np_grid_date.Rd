% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_catch_depth_noaa_np_grid_date.R
\name{plot_catch_depth_noaa_np_grid_date}
\alias{plot_catch_depth_noaa_np_grid_date}
\title{Plot gridded catch by depth vs latitude/longitude (date-range filter; dateline-safe)}
\usage{
plot_catch_depth_noaa_np_grid_date(
  data_o,
  species_name,
  date_min = NULL,
  date_max = NULL,
  region = c("AI", "BS", "GOA"),
  gear = c("Trawl", "Pot", "Longline"),
  facet_gear = FALSE,
  x_axis = c("lat", "lon"),
  x_bin_km = 20,
  depth_bin_m = 10,
  show_titles = TRUE,
  show_label = TRUE
)
}
\arguments{
\item{data_o}{Observer data.frame/data.table with required columns:
GEAR_TYPE, LATDD_END, LONDD_END, RETRIEVAL_DATE, WEIGHT, NMFS_AREA, BOTTOM_DEPTH_FATHOMS.}

\item{species_name}{Character scalar used in the plot title (if titles shown).}

\item{date_min}{Optional start date (inclusive) as "mm/dd/yyyy".}

\item{date_max}{Optional end date (inclusive) as "mm/dd/yyyy".}

\item{region}{Vector: one or more of "AI","BS","GOA","BSWGOA", or numeric NMFS area code(s).}

\item{gear}{Character vector: one or more of "Trawl","Pot","Longline". Default all.}

\item{facet_gear}{If TRUE, facet by gear.}

\item{x_axis}{Which horizontal axis to use: "lat" or "lon".}

\item{x_bin_km}{Horizontal bin size in kilometers (default 20 km).}

\item{depth_bin_m}{Depth bin size in meters (default 10 m).}

\item{show_titles}{If FALSE, remove plot title/subtitle/caption.}

\item{show_label}{If FALSE, remove the upper-right info label.}
}
\value{
A ggplot2 object.
}
\description{
Aggregates observer catch into a 2D grid with depth binned in meters (default 10 m)
and the x-axis (latitude or longitude) binned at a user-specified resolution in km.
The plotted value is the sum of catch weight (metric tons) within each grid cell.
}
\details{
This version is \strong{dateline-safe} for Alaska data: if longitude spans 180°, the function
automatically switches to a continuous longitude representation to avoid the large
artificial break (wraps to 0–360 when that yields the smaller span).

Fill scale is aligned with your gridded map style: viridis + sqrt transform and
legend title "Weight (mt)".
}
