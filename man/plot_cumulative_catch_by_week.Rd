% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cumulative_catch_by_week.R
\name{plot_cumulative_catch_by_week}
\alias{plot_cumulative_catch_by_week}
\title{Plot cumulative catch by week with one line per year}
\usage{
plot_cumulative_catch_by_week(
  catch,
  region = NULL,
  areas = NULL,
  facet_gear = FALSE,
  gears = c("Trawl", "Pot", "Longline", "Jig"),
  show_titles = TRUE,
  title = NULL,
  y_units = "Cumulative catch (mt)",
  show_totals = TRUE
)
}
\arguments{
\item{catch}{data.frame from GET_ALL_CATCH.sql}

\item{region}{Optional region selector: "AI","BS","GOA","BSWGOA"}

\item{areas}{Optional explicit area codes (overrides region)}

\item{facet_gear}{Logical; if TRUE, facet by gear type}

\item{show_titles}{Logical; if FALSE, remove title/subtitle}

\item{title}{Optional plot title}

\item{y_units}{Y-axis label}

\item{show_totals}{Logical; if TRUE, annotate final-year catch in upper-left}

\item{gear}{Character vector of gears to include after recoding}
}
\value{
ggplot object
}
\description{
Uses WEEK_END_DATE (YYYY-MM-DD) to compute ISO week and year.
Produces cumulative weekly catch curves with one line per year.
The most recent year is always drawn in black and thicker.
}
\details{
Gear is taken from FMP_GEAR coded as:
TRW = Trawl, POT = Pot, HAL = Longline, JIG = Jig

Weight is taken from WEIGHT_POSTED (metric tons).
Area is taken from REPORTING_AREA_CODE.
}
