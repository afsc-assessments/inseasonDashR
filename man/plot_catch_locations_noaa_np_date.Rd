% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_catch_locations_noaa_np_date.R
\name{plot_catch_locations_noaa_np_date}
\alias{plot_catch_locations_noaa_np_date}
\title{Plot catch locations on a NOAA-style North Pacific map (date-range filter)}
\usage{
plot_catch_locations_noaa_np_date(
  data_o,
  data_em,
  species_name,
  date_min = NULL,
  date_max = NULL,
  region = c("AI", "BS", "GOA"),
  gear = c("Trawl", "Pot", "Longline"),
  size_range = c(1, 6),
  facet_gear = FALSE,
  show_titles = TRUE,
  show_label = TRUE,
  show_counts = TRUE,
  pad_frac = 0.08
)
}
\arguments{
\item{data_o}{Observer data.frame (GET_CURRENT.sql output)}

\item{data_em}{EM data.frame (GET_EM_CATCH.sql output)}

\item{species_name}{Character string used in the plot title}

\item{date_min}{Optional start date (inclusive) as "mm/dd/yyyy"}

\item{date_max}{Optional end date (inclusive) as "mm/dd/yyyy"}

\item{region}{Character vector: one or more of "AI","BS","GOA","BSWGOA"}

\item{gear}{Character vector: one or more of "Trawl","Pot","Longline"}

\item{size_range}{Numeric length-2 vector giving point size range (metric tons)}

\item{facet_gear}{Logical; if TRUE, facet map by gear type}

\item{show_titles}{Logical; if FALSE, suppress plot title}

\item{show_label}{Logical; if FALSE, suppress upper-right gear label}

\item{show_counts}{Logical; if TRUE, add haul/vessel counts upper-left}

\item{pad_frac}{Fractional padding added around data extent (default 0.08)}
}
\value{
A ggplot object
}
\description{
Plots observer and EM catch locations with transparency scaled by recency
and point size scaled by catch weight (metric tons). The map extent is
automatically zoomed to the spatial extent of the data.
}
\details{
Observer points are always circles; EM points are always triangles.
Robust to cases where one data source or one or more gear types are absent.

Adds upper-left annotations:
\itemize{
\item Hauls: number of plotted rows (points)
\item Vessels: number of unique vessel IDs across both sources
(data_o$VESSEL and data_em$OBS_VESSEL_ID)
}
}
