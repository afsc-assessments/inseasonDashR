% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_catch_figure_date_facet.R
\name{make_catch_map}
\alias{make_catch_map}
\title{Make a NOAA-style North Pacific catch map from AFSC + AKFIN pulls}
\usage{
make_catch_map(
  fsh_sp_label,
  species_name,
  date_min = NULL,
  date_max = NULL,
  plot_type = c("points", "grid"),
  region = c("AI", "BS", "GOA"),
  gear = c("Trawl", "Pot", "Longline"),
  facet_gear = FALSE,
  show_titles = TRUE,
  show_label = TRUE,
  size_range = c(1, 6),
  cell_km = 20,
  pad_frac = 0.08,
  tz = "UTC",
  return_sql = TRUE,
  keep_connections = FALSE,
  return_data = FALSE
)
}
\arguments{
\item{fsh_sp_label}{Numeric/integer species code(s) used in SQL IN clause.}

\item{species_name}{Character string used in the plot title.}

\item{date_min}{Optional start date (inclusive) as \code{"mm/dd/yyyy"}.}

\item{date_max}{Optional end date (inclusive) as \code{"mm/dd/yyyy"}.}

\item{plot_type}{Either \code{"points"} or \code{"grid"}.}

\item{region}{Character vector of regions: \code{"AI"}, \code{"BS"}, \code{"GOA"}, \code{"BSWGOA"}.}

\item{gear}{Character vector of gear types: \code{"Trawl"}, \code{"Pot"}, \code{"Longline"}.}

\item{facet_gear}{Logical; if TRUE, facet the plot by gear (passed to plot funcs).}

\item{show_titles}{Logical; if FALSE, remove titles (passed to plot funcs).}

\item{show_label}{Logical; if FALSE, remove upper-right label (passed to plot funcs).}

\item{size_range}{Points-only: numeric length-2, point size range for weight scaling (mt).}

\item{cell_km}{Grid-only: grid cell size in km.}

\item{pad_frac}{Map padding fraction around data extent (passed to plot funcs where supported).}

\item{tz}{Passed to \code{get_catch_data()} for date parsing.}

\item{return_sql}{Passed to \code{get_catch_data()} (default TRUE).}

\item{keep_connections}{If FALSE (default), DB connections are closed on exit.}

\item{return_data}{If TRUE, return a list including data + SQL in addition to the plot.}
}
\value{
By default, a \code{ggplot} object.
If \code{return_data=TRUE}, returns a list with \code{$plot}, \code{$data_o}, \code{$data_em},
and optionally \code{$sql}.
}
\description{
Wrapper around:
\itemize{
\item \code{connect_afsc_akfin()}
\item \code{get_catch_data()}
\item \code{plot_catch_locations_noaa_np_date()} (points)
\item \code{plot_catch_locations_noaa_np_grid_date()} (grid)
}
}
\details{
This wrapper:
\itemize{
\item Connects to AFSC and AKFIN using keyring credentials
\item Pulls observer + EM catch data for a species and date range
\item Produces either a point map or a gridded map
}
}
