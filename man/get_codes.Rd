% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_codes.R
\name{get_codes}
\alias{get_codes}
\title{Retrieve code table rows for one or more species}
\usage{
get_codes(con, spec, sql_dir = "sql")
}
\arguments{
\item{con}{A DBI connection object (e.g., your AFSC connection).}

\item{spec}{Integer or numeric vector of agency species codes to filter on.}

\item{sql_dir}{Character path to the directory containing GET_CODES.sql.
Default is "sql".}
}
\value{
A data.frame containing the code table rows returned by the query.
}
\description{
Reads a SQL template file (GET_CODES.sql), injects one or more agency species
codes, and runs the query against a provided database connection using the
\code{sql_filter()} and \code{sql_run()} utilities.
}
\examples{
\dontrun{
afsc <- db_connect("afsc")
codes <- get_codes(afsc, spec = 202)
codes2 <- get_codes(afsc, spec = c(202, 203))
}

}
