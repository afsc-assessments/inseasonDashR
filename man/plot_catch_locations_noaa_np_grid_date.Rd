% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_catch_locations_noaa_np_grid_date.R
\name{plot_catch_locations_noaa_np_grid_date}
\alias{plot_catch_locations_noaa_np_grid_date}
\title{Plot gridded catch locations on a NOAA-style North Pacific map (date-range filter)}
\usage{
plot_catch_locations_noaa_np_grid_date(
  data_o,
  data_em,
  species_name,
  date_min = NULL,
  date_max = NULL,
  region = c("AI", "BS", "GOA"),
  gear = c("Trawl", "Pot", "Longline"),
  cell_km = 20,
  pad_frac = 0.08,
  facet_gear = FALSE,
  show_titles = TRUE,
  show_label = TRUE
)
}
\arguments{
\item{data_o}{Observer data.frame}

\item{data_em}{EM data.frame}

\item{species_name}{Character string used in the plot title (if titles shown)}

\item{date_min}{Optional start date (inclusive) as "mm/dd/yyyy"}

\item{date_max}{Optional end date (inclusive) as "mm/dd/yyyy"}

\item{region}{Vector: one or more of "AI","BS","GOA","BSWGOA", or NMFS area/s.}

\item{gear}{Character vector: one or more of "Trawl","Pot","Longline". Default all.}

\item{cell_km}{Grid cell size in kilometers (default 20)}

\item{pad_frac}{Padding around data extent for grid creation (default 0.08)}

\item{facet_gear}{If TRUE, facet the gridded map by gear (aggregates by GRID_ID + GEAR).}

\item{show_titles}{If FALSE, remove plot title/subtitle/caption (clean figure).}

\item{show_label}{If FALSE, remove the upper-right info label.}
}
\value{
A ggplot object
}
\description{
Aggregates observer and EM catch data into a regular grid (default 20 km)
in a North Pacific Lambert Azimuthal Equal-Area projection, then plots
grid-cell totals on a NOAA-style basemap.
}
\details{
Date filtering is applied (inclusive) using:
\itemize{
\item Observer: RETRIEVAL_DATE
\item EM:       RETRIEVAL_END_DATE
}
}
